// ignore_for_file: constant_identifier_names

import 'package:flutter/material.dart';

// common

const WHITE_COLOR = Colors.white;

// info

const String INFO = 'Нажмите на любую точку экрана, чтобы построить угол';

// painter

// there are have not got dot radius on Figma layout so I choosed:
const DOT_RADIUS = 1.3;
// there are have not got vertex countur radius on Figma layout so I choosed:
const double OUTSIDE_VERTEX_SCALE_ABOUT_UNIT = 4.0;
// there are have not got vertex radius on Figma layout so I choosed:
const INSIDE_VERTEX_SCALE_ABOUT_UNIT = 4 / 0.7;
// there are have not got vertex radius on Figma layout so I choosed:
const INSIDE_POLYGON_VERTEX_SCALE_ABOUT_UNIT = 4 / 0.85;
const MARKING_FREQUENCY = 15;
const POINTER_SCALE_ABOUT_UNIT = 1.68;
const BACKGROUND_COLOR = Color.fromRGBO(227, 227, 227, 1);
const DOTS_COLOR = Color.fromRGBO(0, 151, 238, 0.394);
const VERTEX_COLOR = Color.fromRGBO(0, 152, 238, 1);
const TEXT_COLOR = Color.fromRGBO(0, 152, 238, 1);
const POLYGON_VERTEX_COUNTUR = Color.fromRGBO(125, 125, 125, 1);
const SIDES_COLOR = Colors.black;
const ATTENTION_SIDES_COLOR = Colors.red;

// buttons

const SWITCH_STATE_BUTTON_SIZE = Size(80.23, 31);
const SWITCH_STATE_BUTTON_COLOR = Color.fromRGBO(246, 246, 246, 1);
const SWITCH_STATE_BUTTON_BORDER_RADIUS = 5.31;

const DIVIDER_WIDTH = 0.44;
const DIVIDER_HEIGHT = 11.96;
const DIVIDER_COLOR = Color.fromRGBO(125, 125, 125, 1);

// cancel

const CANCEL_BORDER_RADIUS = 13.0;
const CANCEL_INNER_BORDER_RADIUS = 11.0;
const CANCEL_INNER_MARGIN =
    EdgeInsets.only(top: 12, bottom: 11.5, left: 12, right: 12);
const CANCEL_INNER_PADDING = EdgeInsets.symmetric(vertical: 7);
// const WHITE_COLOR = Colors.white;
const CANCEL_BUTTON_COLOR = Color.fromRGBO(227, 227, 227, 1);
